<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لیست سایت‌های ثبت شده</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #1a1a2e, #16213e);
            color: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 20px;
        }
        .sites-container {
            max-width: 800px;
            margin: 30px auto;
        }
        .site-card {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }
        .site-card:hover {
            transform: translateY(-5px);
        }
        .site-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }
        .site-title {
            font-weight: bold;
            font-size: 18px;
            color: #9d50bb;
        }
        .site-url a {
            color: #6e48aa;
            text-decoration: none;
        }
        .site-url a:hover {
            text-decoration: underline;
        }
        .site-desc {
            margin-top: 10px;
            line-height: 1.6;
            color: #ddd;
        }
        .like-btn {
            background: none;
            border: none;
            color: #dc3545;
            font-size: 24px;
            cursor: pointer;
            transition: transform 0.2s;
        }
        .like-btn:hover {
            transform: scale(1.2);
        }
        .like-btn.liked {
            color: #dc3545;
        }
        h2 {
            text-align: center;
            margin-bottom: 30px;
            color: #fff;
            text-shadow: 0 2px 4px rgba(0,0,0,0.3);
        }
        .no-sites {
            text-align: center;
            padding: 30px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="sites-container">
            <h2>لیست سایت‌های ثبت شده</h2>
            
            {% if sites %}
                {% for site in sites %}
                    <div class="site-card">
                        <div class="site-header">
                            <div class="site-title">{{ site[0] }}</div>
                            <button class="like-btn">❤️</button>
                        </div>
                        <div class="site-url">
                            <a href="{{ site[1] }}" target="_blank">{{ site[1] }}</a>
                        </div>
                        <div class="site-desc">{{ site[2] }}</div>
                    </div>
                {% endfor %}
            {% else %}
                <div class="no-sites">
                    <p>هنوز هیچ سایتی ثبت نشده است.</p>
                    <a href="/sabt/" class="btn btn-primary">ثبت سایت جدید</a>
                </div>
            {% endif %}
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.querySelectorAll('.like-btn').forEach(btn => {
            btn.addEventListener('click', function() {
                this.classList.toggle('liked');
                if (this.classList.contains('liked')) {
                    this.textContent = '❤️';
                } else {
                    this.textContent = '🤍';
                }
            });
        });
    </script>
</body>
</html>
